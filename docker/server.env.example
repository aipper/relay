# Required
JWT_SECRET=change-me
ADMIN_USERNAME=admin

# Set ONE of:
# - ADMIN_PASSWORD (entrypoint will derive ADMIN_PASSWORD_HASH)
# - ADMIN_PASSWORD_HASH (preferred for production)
ADMIN_PASSWORD=123456
# If you set ADMIN_PASSWORD_HASH manually, quote it to avoid '$' parsing issues in some tooling:
# ADMIN_PASSWORD_HASH='$argon2id$v=19$m=19456,t=2,p=1$...'

# Optional
# Tip: generate a random JWT secret:
#   bash scripts/gen-jwt-secret.sh
# BIND_ADDR=0.0.0.0:8787
# DATABASE_URL=sqlite:/data/server.db
# WEB_DIST_DIR=/app/web/dist
# REDACTION_EXTRA_REGEX=your-regex-1,your-regex-2
# RUST_LOG=relay_server=info
# SERVER_LOG_PATH=/data/relay-server.log
