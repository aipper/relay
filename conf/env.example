# Server
BIND_ADDR=0.0.0.0:8787
DATABASE_URL=sqlite:data/server.db
JWT_SECRET=change-me-to-a-long-random-string
ADMIN_USERNAME=admin
# Generate a hash via: `cargo run -p relay-server -- --hash-password 'your-password'`
ADMIN_PASSWORD_HASH=$argon2id$v=19$m=19456,t=2,p=1$...$...

# Redaction
STORE_RAW_INPUT=false
REDACTION_EXTRA_REGEX=

# hostd (example)
# LOCAL_UNIX_SOCKET=/run/relay/hostd.sock
# SERVER_BASE_URL=wss://your-server.example.com
# HOST_ID=host-01
# HOST_TOKEN=change-me
# SPOOL_DB_PATH=data/hostd-spool.db
