# plan: web-unit-more（stores/utils 剩余纯函数单测）

## Bindings
Change_ID: web-unit-more
Req_ID: WEB-UNIT-003
Problem_ID: PWA-UNIT-003
Contract_Row: WEB-UNIT-003
Plan_File: .aiws/plan/2026-09-17_19-40-00-web-unit-more.md
Evidence_Path: .aiws/changes/web-unit-more/evidence/
Change_Type: config-docs

## Goal
`utils-extra.test.ts` 覆盖 12 个未测纯函数，全绿。

## Non-goals
不改产品逻辑；不测 relay-store；不碰 e2e。

## Scope
新增：`web/src/lib/stores/utils-extra.test.ts`。其余只读。

## Plan
- [x] 步骤1：起 change + CSV + proposal/tasks/plan 落盘 — expect change/web-unit-more 分支与工件就绪
- [ ] 步骤2：worker 写 `utils-extra.test.ts`（12 函数：uid 格式/dataAny 取值/isRecord 守卫/formatRelativeTime 分段/formatAbsTime 非法回退/connLabel 全映射/sessionTitle-sessionSummary 截断/looksLikeTuiAnsi/isLikelyTuiToolName/codexStructuredEventText 合法与非法/renderOutputHtml 转义与高亮） — expect `bash -c "cd web && bun run test:unit"` 全绿
- [ ] 步骤3：`bash -c "cd web && bun run build"` exit 0 — expect 构建通过
- [ ] 步骤4：证据 + review 落盘 — expect evidence/ 与 review/ 就绪
- [ ] 步骤5：提交并 finish — expect 合入 main

## Verify
- `bash -c "cd web && bun run test:unit"` — expect 全绿（含既有 42 用例）
- `bash -c "cd web && bun run build"` — expect exit 0

## Risks & Rollback
只新增测试文件；回滚即删文件。formatRelativeTime 依赖 Date.now，用固定偏移构造输入避免 flake。

## Evidence
.aiws/changes/web-unit-more/evidence/
