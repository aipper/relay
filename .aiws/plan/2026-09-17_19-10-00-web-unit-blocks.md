# plan: web-unit-blocks（blocks 单测覆盖）

## Bindings
Change_ID: web-unit-blocks
Req_ID: WEB-UNIT-002
Problem_ID: PWA-UNIT-002
Contract_Row: Req_ID=WEB-UNIT-002, Problem_ID=PWA-UNIT-002
Plan_File: .aiws/plan/2026-09-17_19-10-00-web-unit-blocks.md
Evidence_Path: .aiws/changes/web-unit-blocks/evidence/
Change_Type: config-docs

## Goal
reduceToBlocks/reconcileBlocks 被单测锁定。

## Non-goals
不改产品逻辑；relay-store 直测仍跳过。

## Scope
新增 web/src/lib/blocks/reduce.test.ts、reconcile.test.ts。

## Plan
- [x] Step 1 新建 `web/src/lib/blocks/reduce.test.ts`（151 行）：覆盖 tool.call/result 配对合并、ok 三态判定、run.output 非重要过滤、assistant 输出合并、pinned request 跳过 — expect 该文件用例全绿
- [x] Step 2 新建 `web/src/lib/blocks/reconcile.test.ts`（76 行）：覆盖相同块保持引用、内容变更替换、新增块、顺序变化、空 prev — expect 该文件用例全绿
- [x] Step 3 运行 `bash -c "cd web && bun run test:unit"` 全量回放 — expect 3 files / 42 tests passed；运行 `bash -c "cd web && bun run build"` — expect exit 0
- [x] Step 4 落盘 `.aiws/changes/web-unit-blocks/evidence/blocks-unit.md` 并写 `review/codex-review.md` — expect 证据可回放

## Verify
- bash -c "cd web && bun run test:unit" → 预期 3 files / 42 tests passed，实际已达标
- bash -c "cd web && bun run build" → 预期 exit 0 且 PWA precache 正常，实际已达标

## Risks & Rollback
用例与实现语义分歧时以实现为准；删测试文件即回滚。

## Evidence
.aiws/changes/web-unit-blocks/evidence/blocks-unit.md
