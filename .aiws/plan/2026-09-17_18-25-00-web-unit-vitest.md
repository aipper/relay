# Plan: web 前端 vitest 单测基建 + 首批用例

- Change_ID: web-unit-vitest
- Req_ID: WEB-UNIT-001
- Contract_Row: WEB-UNIT-001
- Plan_File: .aiws/plan/2026-09-17_18-25-00-web-unit-vitest.md
- Evidence_Path: .aiws/changes/web-unit-vitest/evidence/
- Change_Type: config-docs

## Goal

web 跑得起 `bun run test:unit`（vitest），首批用例锁定 relay-store seq 语义，全绿。

## Non-goals

- 不改 `web/src` 产品逻辑；不设覆盖率门禁；不重构 relay-store。

## Scope

- `web/package.json`（devDep + script）
- `web/vitest.config.ts`（新增）
- `web/src/lib/stores/relay-store.seq.test.ts`（新增）

## Plan

- [x] Step 1: change 启动 + proposal/tasks/plan 落盘（本文件）
- [ ] Step 2: worker 安装 vitest 并配好 config + test:unit 脚本
- [ ] Step 3: worker 写 seq 单测（去重/回填合并/截断 ≥5 用例）
- [ ] Step 4: `bun run test:unit` 全绿，落盘 evidence/unit-baseline.md
- [ ] Step 5: `bun run build` exit 0 回归
- [ ] Step 6: ws-review → commit → finish → archive

每步期望结果：命令 exit 0；单测 fail 即停，不进下一步。

## Verify

- `cd web && bun run test:unit` → 全绿（tests ≥ 5, failed 0）
- `cd web && bun run build` → exit 0

## Risks & Rollback

- `.svelte.ts` 在 vitest 下编译失败 → 复用 vite svelte 插件管线；不行则改测纯函数抽取
- 回滚：`git revert` 本 change 提交

## Evidence

- `.aiws/changes/web-unit-vitest/evidence/unit-baseline.md`
- 本计划文件
