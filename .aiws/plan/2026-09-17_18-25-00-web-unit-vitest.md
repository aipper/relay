# Plan: web 前端 vitest 单测基建 + 首批用例

- Change_ID: web-unit-vitest
- Req_ID: WEB-UNIT-001
- Contract_Row: WEB-UNIT-001
- Plan_File: .aiws/plan/2026-09-17_18-25-00-web-unit-vitest.md
- Evidence_Path: .aiws/changes/web-unit-vitest/evidence/
- Change_Type: config-docs

## Goal

web 跑得起 `bun run test:unit`（vitest），首批用例覆盖纯函数，全绿。

## Non-goals

- 不改 `web/src` 产品逻辑；不设覆盖率门禁；不重构 relay-store。

## Scope

- `web/package.json`（devDep + script）
- `web/vitest.config.ts`（新增）
- `web/src/lib/stores/utils.test.ts`（新增；原计划 relay-store.seq.test.ts，因 runes+浏览器依赖换路，见 evidence/unit-baseline.md）

## Plan

- [x] Step 1: `aiws change start web-unit-vitest --hooks` + proposal.md/tasks.md/本文件落盘（文件：.aiws/changes/web-unit-vitest/{proposal,tasks}.md）
- [x] Step 2: worker 执行 `cd web && bun add -d vitest`，新增 `web/vitest.config.ts`（node 环境，include src/**/*.test.ts），`web/package.json` 加 `"test:unit": "vitest run"` 脚本
- [x] Step 3: worker 新增 `web/src/lib/stores/utils.test.ts`，覆盖 utils.ts 纯函数（truncateTail/truncateHead/sanitizeTerminalOutput 等 ≥5 用例，实落 26 用例）
- [x] Step 4: 运行 `cd web && bun run test:unit` 全绿，落盘 `.aiws/changes/web-unit-vitest/evidence/unit-baseline.md`
- [x] Step 5: 运行 `cd web && bun run build` 回归（exit 0，PWA precache 5 entries）
- [x] Step 6: 通用 review 落盘 review/codex-review.md → commit（67060c1 起三提交）→ `aiws change finish` 合入 main → archive

每步期望结果：命令 exit 0；单测 fail 即停，不进下一步。

## Verify

- 运行 `cd web && bun run test:unit` → 预期：Test Files 1 passed，Tests 26 passed，failed 0（实测 Duration ~0.3s）
- 运行 `cd web && bun run build` → 预期：exit 0，PWA precache 5 entries 正常生成

## Risks & Rollback

- `.svelte.ts` 在 vitest 下编译失败 → 复用 vite svelte 插件管线；不行则改测纯函数抽取（实际走的即此路）
- 回滚：`git revert` 本 change 提交

## Evidence

- `.aiws/changes/web-unit-vitest/evidence/unit-baseline.md`
- 本计划文件
