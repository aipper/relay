# Plan: 吸收 paseo 回显与指令设计（全序列）

## Bindings

Change_ID: paseo-absorb-v1
Req_ID: PAS-000
Problem_ID: 无（`issues/` 目录不存在，未建问题工件）
Contract_Row: PAS-000, PAS-001, PAS-002, PAS-003, PAS-004
Plan_File: .aiws/plan/2026-09-17_11-04-38-paseo-absorb.md
Evidence_Path: .aiws/changes/paseo-absorb-v1/evidence/
- `Change_Type`: `full-stack`（`frontend-logic` + `backend-api`）

## Goal

按性价比顺序把 paseo（`getpaseo/paseo@6830c46`）四项设计落到 relay，且全部保持协议后向兼容（只加字段、不改名、不删字段）：
1. PWA 本地 slash 截断（`/quit /clear` 类精确匹配，不进 `run.send_input`）
2. server 侧 5ms 前沿输出合并（首包立即发，burst 合并，降 `run.output` 包数）
3. 审批结构化 `actions[]`（`run.permission_requested.data` 加字段，PWA 多按钮、CLI 按 id 选）
4. `seq` 可见性 + 快照追赶（复用已有 per-`run_id` `seq` 与 `run.ack`/spool，不发明新 rev；web 断线按 `seq` 查缺口）

## Non-goals

- 不做二进制复用帧（`[opcode, slot, payload]`）：当前量级瓶颈在包数不在编码，JSON 够用。
- 不引入 provider-adapter 层、不按 `cwd` 存 JSON、不做 word-paced reveal、不做 tool-call 级审批粒度。
- 不改 `codex` 之外的 runner 启动语义；不碰 E2E 加密；不改 `run.stop` 语义。
- 不在这轮修 `relay.aipper.de:8787` 不可达与线上 hostd 接入问题（另起 change）。

## Scope

确定要动（按 phase）：

- Phase A 本地 slash：`web/src/lib/components/ChatInput.svelte`
- Phase B 合并器：`server/src/main.rs`（fan-out/广播点）、`server/src/db.rs`（`insert_event`/`list_message_events` 路径）、`protocol/src/lib.rs`（若有共享事件类型需确认）
- Phase C 审批 actions：`docs/protocol.md`、`protocol/src/lib.rs`、`server/src/main.rs`、`server/src/db.rs`、`hostd/src/runners/*.rs` + `hostd/src/local_api.rs`（`permission_requested` 发射点）、`web/src/lib/components/ApprovalCard.svelte`、`web/src/lib/components/ApprovalModal.svelte`、`web/src/lib/components/SessionDetail.svelte`、`web/src/lib/stores/relay-store.svelte.ts`
- Phase D seq/快照：`server/src/main.rs`（messages API：`include_output`/`after_seq` 语义）、`web/src/lib/stores/relay-store.svelte.ts`（订阅 gap 检测 + 回填）、`docs/protocol.md`
- 验证与回归：`scripts/e2e.sh`、`scripts/dev-up.sh`、`web/tests/e2e/`（沿用 mock，不新增生产依赖）

TBD（进入 dev 前由 subagent  pin 到行）：
- 合并器 exact 插入点：server fan-out vs hostd `run_manager.rs`/`spool.rs` 二选一（倾向 server 侧，hostd spool 保持原始 chunk 以保 replay 完整性；需实测确认）
- `run.output` 是否带 `rev/seq` 下发到 web（envelope 已有 `seq`，确认 web 是否已消费）

## Plan

复杂度：`complex`（4 phase、全栈、协议兼容约束）。以下 8 步均为已完成事项（close-out 归档口径，子步骤细节见各 phase 证据）：

- [x] A ChatInput 本地 slash 守卫 + Playwright 断言（`/quit` 零新增 `run.input`）——证据 phase-a.md
- [x] B1 合并器插入点 pin（server 推送侧，spool 保持原始 chunk）
- [x] B2 5ms 前沿合并器实现（`server/src/coalescer.rs`）+ `cargo test`
- [x] B3 burst 断言（首包 `<50ms`，包数远小于行数且文本全对）——证据 phase-b.md
- [x] C1 协议扩展（`actions[]/suggestions[]/selected_action_id`，只加字段）+ 兼容单测——对应 PAS-003
- [x] C2 后端透传 + C3 PWA 多按钮渲染 + approve round-trip 断言——证据 phase-c.md
- [x] D1 messages API `after_seq` + D2 web seq 高水位回填（curl + build 验证）——对应 PAS-004
- [x] D3 burst 中杀 WS 重连断言（seq 无 gap 无重复）——证据 phase-d.md

## Verify

与 `Change_Type=full-stack` 匹配（Rust + Playwright + e2e 全要）。期望结果（expected）：以下命令全部退出码 0；Playwright phase-b/c/d 断言各 1 passed；标准 mock 套件 69/99（30 失败为基线预置、与本 change 无关）：

- `cargo fmt --check` → 退出码 0
- `cargo clippy --all-targets --all-features` → 退出码 0（warning 可接受）
- `cargo test --workspace` → 退出码 0
- `bash scripts/e2e.sh` → 含 `[e2e] ok: input idempotency` 且退出码 0
- 隔离栈：`bash scripts/dev-up.sh --port 8790` → `curl --noproxy "*" http://127.0.0.1:8790/health` OK
- `cd web && bun run build` → 成功（PWA 相关失败时按 REQUIREMENTS 先试 `RELAY_DISABLE_PWA=1`）
- Playwright（本地 `~/.cache/ms-playwright/chromium-1208/chrome-linux64/chrome`，`executablePath` + `--no-sandbox`，连 `8790` 栈 + web dev `5173`）：
  - `/quit` 零 `run.input` 新增
  - `echo hello` 首包 `<50ms`
  - `seq 1 100` 包数远小于行数且文本全对
  - burst 中重连 `seq` 无 gap 无重复
  - 审批 `actions[]` 一轮 approve 生效

## Risks & Rollback

- 协议兼容：只加字段；web/CLI/hostd 旧版本必须忽略未知字段。回滚：按文件 `git checkout -- <file>`，协议文档同步回退。
- 合并器放错位置会丢 replay 完整性：默认放 server 推送侧，spool 保持原始 chunk；若实测证明放 hostd 更优，需补 spool 回放语义说明。回滚：删合并器调用点，恢复直通。
- 工作区当前 dirty（含 `.opencode/*`、`AI_*.md`、`web/...SessionDetail/SessionList/ChatInput/LoginPage` 改动），未建 `change/<id>` worktree；`plan/` 直接写在当前工作区。进入 dev 前必须先 `commit/stash` 再 `aiws change start paseo-absorb-v1 --hooks`，否则上下文门禁会拦。
- `.aiws/requirements/requirements-issues.csv` 已补 PAS-001~PAS-004（2026-09-17 close-out），`Contract_Row` 不再是 TBD。
- 不做 `git reset --hard` / `rm -rf` / `clean -fdx` 类破坏性操作；重启涉及 `server/hostd` 时遵守 `AI_PROJECT.md §7`（停 runs → 清 `relay-run-*` tmux → 重编重启 → 健康检查）。

## Evidence

- 本计划文件（已落盘）
- `.aiws/changes/paseo-absorb-v1/proposal.md`、`tasks.md`、`evidence/phase-a.md`、`evidence/phase-b.md`、`evidence/phase-c.md`、`evidence/phase-d.md`、`evidence/verification.jsonl`、`evidence/verify-before-complete.md`
