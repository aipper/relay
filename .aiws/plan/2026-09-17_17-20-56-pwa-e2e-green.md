# Plan: PWA e2e 套件全绿（修 30 个预置失败）

## Bindings

Change_ID: pwa-e2e-green
Req_ID: PWA-E2E-001
Problem_ID: PWA-E2E-001
Contract_Row: PWA-E2E-001
Plan_File: .aiws/plan/2026-09-17_17-20-56-pwa-e2e-green.md
Evidence_Path: .aiws/changes/pwa-e2e-green/evidence/
Change_Type: frontend-logic（测试 + 可能的 PWA 小修）

## Goal

`cd web && playwright test` 99/99 全过（当前 69 通过 / 30 失败，同一 10 用例 × desktop/tablet/mobile）。

## Non-goals

- 不改产品行为语义：app 侧 legitimate 的 UI 只修测试选择器，不为过测试改功能。
- 不修 `scripts/e2e.sh` 的 codex/opencode 工具不匹配（另起 change）。
- 不碰 server/hostd/Rust。

## Scope

失败清单（待 dev 逐个 pin 根因，分类 test-bug 修测试 / app-bug 修 PWA）：

- `session-switch` ×3：已知，`切换` strict-mode（sidebar-toggle"切换侧栏位置" vs  secondary"切换"），基线 8fea4e4 引入。
- `auth` 登录页面渲染：同页其它断言过，疑 heading"Relay"/"刷新后保持登录" 文案漂移。
- `auth` 记住登录状态（30s 超时）：疑 remember-me/localStorage 语义变化。
- `app` 会话搜索过滤、`interaction` 审批卡片、`session-fork` ×2、`session-new` 留空新建：未知，dev 内单测复现后分类。
- 可能涉及：`web/tests/e2e/*.spec.ts`、`web/tests/e2e/fixtures/*`、`web/src/lib/components/*`、`web/src/lib/pages/*`（仅选择器/可访问性层面）。

## Plan

复杂度：`medium`（10 用例、单域、根因已有 1 个 pin，其余需复现分类）。

- [x] 1 诊断：`cd web && ./node_modules/.bin/playwright test --project=desktop tests/e2e/auth.spec.ts:4` 单跑复现，逐个记录错误原文并分类为 test-bug（10/10 全为测试侧漂移）——文件 web/tests/e2e/*.spec.ts，证据 .aiws/changes/pwa-e2e-green/evidence/e2e-green.md
- [x] 2 修 web/tests/e2e/session-switch.spec.ts ×3：`name:'切换'` → `exact:true`，避 sidebar-toggle（web/src/lib/components/PageShell.svelte）多匹配
- [x] 3 修 web/tests/e2e/auth.spec.ts ×2：删登录页“刷新后保持登录”过期断言（已搬 SettingsPage.svelte:59-61）；记住登录改为登录后进设置页断言再验 localStorage relay.auth.v1
- [x] 4 修 web/tests/e2e/app.spec.ts 搜索过滤：`getByPlaceholder(/搜索|search/i)` → `getByRole('searchbox',{name:'搜索会话'})`；修 web/tests/e2e/interaction.spec.ts 审批卡：`dialog` 断言 → 同意/拒绝按钮可见（first() 避 modal+内联双命中）
- [x] 5 修 web/tests/e2e/session-fork.spec.ts ×2：长文本断言 → `placeholder 'ses_xxx 或留空新建'`；修 web/tests/e2e/session-new.spec.ts：`CWD（可选，主机路径）` → `placeholder '/path/to/project'`；补 web/tests/e2e/fixtures/app.ts mock WebSocket 静态常量 OPEN/CONNECTING/CLOSING/CLOSED
- [x] 6 全量 `cd web && ./node_modules/.bin/playwright test`（99 tests, 4.7m）→ 99 passed 0 failed，`web/test-results/.last-run.json` 落盘——证据 .aiws/changes/pwa-e2e-green/evidence/e2e-green.md + /tmp/pw-green.log

## Verify

与 `Change_Type=frontend-logic` 匹配。期望结果：

- `cd web && ./node_modules/.bin/playwright test` → 退出码 0，`99 passed`
- `cd web && bun run build` → 成功（若动了 PWA 源码）
- `cargo` 门禁不跑（未动 Rust）；`scripts/e2e.sh` 不跑（codex 不匹配是已知另案）

## Risks & Rollback

- 修测试时掩盖真实 app 回归：每处 app-bug 分类必须有错误原文 + 行为解释；只改选择器时注明 app 行为 legitimate 的理由。
- 3 viewport 放大：同一用例 ×3，修 desktop 后必须全量确认 tablet/mobile。
- 回滚：按文件 `git checkout -- web/tests web/src`。
- 不做 `git reset --hard` / `rm -rf` 破坏性操作。

## Evidence

- 本计划文件
- `.aiws/changes/pwa-e2e-green/evidence/failures.md`、`evidence/green.md`
- `web/test-results/.last-run.json`（全绿）
